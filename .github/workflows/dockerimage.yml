name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Test Update on Ubuntu
    - run: |
        sudo chmod +x LinuxUpdate.sh
        sudo ./LinuxUpdate.sh

    
